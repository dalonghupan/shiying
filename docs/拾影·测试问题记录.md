# 拾影·测试问题记录

**用途**：记录功能测试、性能测试中发现的问题，作为后续修改的依据
**状态说明**：🔴 待修复 | 🟡 修复中 | 🟢 已修复

---

## 问题列表

| # | 状态 | 模块 | 严重程度 | 问题描述 | 修复方案 |
|---|------|------|----------|----------|----------|
| 1 | 🟢 | 启动 | 高 | 直接 `python3 main.py` 报错 `No module named 'PyQt6'`，需使用 venv | 启动脚本或文档说明 |
| 2 | 🟢 | AI模型配置 | 高 | 接口地址为空，点击测试连接，程序中断退出 | 已添加 try-catch 和 close 处理 |
| 3 | 🟢 | AI模型配置 | 低 | 配置默认显示在页面，容易误触发 | AI 模型配置组默认隐藏，选择 AI 模式时才显示 |
| 4 | 🟢 | 筛选照片 | 中 | 选择图片目录后，加载图片一直卡在11% | 修复批次触发逻辑，错误也计入已处理数 |
| 5 | 🟢 | 筛选照片 | 中 | 选择图片目录后，图片加载数量不对。例如：/Users/yueting/Desktop/Alan/images/big 这个目录下有161张图片，但是程序筛选出来只有159张 | RGBA PNG 转 JPEG 失败，添加 RGB 转换 |
| 6 | 🟢 | 筛选照片 | 中 | 点击开始筛选按钮后，照片评完分，点击选择框，导出已选按钮不能点击 | 筛选完成后启用导出按钮 |
| 7 | 🟢 | 筛选模式 | 中 | 使用基础算法筛选照片，完成评分后，再选择AI大模型，评分直接变成AI | 评分来源与分数分开存储，切换模式不覆盖 |
| 8 | 🟢 | AI模型配置 | 高 | AI地址配置为空，点击测试连接，程序中断并退出 | 添加 http/https 协议头校验 |
| 9 | 🟢 | AI模型配置 | 高 | 配置deepseek，点击开始筛选，程序中断并退出 | 模型名可配置，增强错误处理和 JSON 解析容错 |
| 10 | 🟢 | 日志系统 | 中 | 运行无日志输出，异常被静默吞掉，排查困难 | 新增 logging 模块，日志写入 ~/.shiying/logs/shiying.log |
| 11 | 🟢 | AI模型配置 | 高 | 用户输入错误的 API 地址（如 https://api.deepseek.com/anthropic），请求发到错误端点返回 404 | 添加 URL 格式校验，提示正确的 /v1/chat/completions 格式 |
| 12 | 🟢 | AI模型配置 | 高 | 使用 deepseek-chat（纯文本模型）发送图片请求，API 返回 400 Bad Request | 添加视觉模型检测，非视觉模型给出明确提示 |
| 13 | 🟢 | AI模型配置 | 高 | 使用 deepseek-vl 模型仍返回 400 Bad Request，缺少 API 错误响应详情 | DeepSeek API 不支持图片分析，已更新错误提示和 UI 说明 |
| 14 | 🟢 | 阈值 | 中 | 鼠标点击阈值条，阈值没有锁定鼠标所在阈值条的值。预期应为鼠标落点的值为新的阈值 | 自定义 JumpSlider，点击轨道直接跳转 |
| 15 | 🟢 | 筛选图片 | 高 | 评分高于所设置的阈值，图片被选中。预期为选择照片只能用户选择 | 移除阈值变化时的自动选中逻辑 |
| 16 | 🟢 | 筛选图片 | 高 | 当筛选的图片过少时，评分、选择框没有跟随图片。预期要求评分、选择框跟随图片 | 提取 _relayout_grid()，列自动拉伸，resize 时重新布局 |
| 17 | 🟢 | 阈值 | 中 | 阈值条无法使用鼠标进行拖动设计。预期为允许使用鼠标点击进行拖动阈值条设置阈值 | JumpSlider 调用 super() 保留拖动功能 |
| 18 | 🟢 | 筛选图片 | 高 | 问题同16，当筛选图片过少时，评分和选择框的位置被上下拉伸了距离。预期为紧跟图片 | 网格组件顶部对齐 + Maximum 高度策略 |
| 19 | 🟢 | 筛选图片 | 高 | 点击统计信息里的信息项，照片显示未按当前排序机制进行排序，当图片过少（原目录下有161张图片，已选择5张，点击已选择，图片展示列表未重新排序，并有空白占位），出现空白占位 | _relayout_grid 只排列可见卡片，筛选后触发重新布局 |
| 21 | 🟢 | 筛选图片 | 高 | 选择目录，图片加载完成后，没有立即显示图片。点击统计信息的总图片项后才显示，而且整个过程很慢。优化一下 | add_image 追加不触发重排，加载完后 finalize_layout 一次性布局 |

---

## 测试环境

- **操作系统**：macOS
- **Python 版本**：3.11（venv）
- **测试方式**：`./venv/bin/python main.py`

---

## 使用说明

1. 测试时发现问题，追加到上方表格
2. 严重程度：高（阻断使用）/ 中（功能异常）/ 低（体验问题）
3. 修复后将状态改为 🟢，并在下方补充修复记录
4. 批量修复时按模块分组处理

---

## 修复记录

### 2026-05-27

**#21 加载图片不显示且慢**
- 文件：`ui/preview_panel.py`, `ui/main_window.py`
- 原因：`add_image` 每次调用 `_relayout_grid()`（O(n)），161 张图片导致 O(n²) 性能问题；加载过程中全量重排导致 UI 卡顿
- 修复：`add_image` 改为直接追加卡片到网格末尾（O(1)）；新增 `finalize_layout()` 方法，`_on_all_loaded` 中调用一次完成最终布局

**#19 筛选后空白占位和排序问题**
- 文件：`ui/preview_panel.py`
- 原因：`_relayout_grid()` 排列所有卡片（包括隐藏的），隐藏卡片在网格中留下空位；`show_all/show_only_selected/show_only_above_threshold` 切换可见性后未触发重新布局
- 修复：`_relayout_grid()` 改为只排列 `isVisible()` 的卡片；三个 show 方法末尾调用 `_relayout_grid()`

**#18 少图片评分/选择框被拉伸**
- 文件：`ui/preview_panel.py`
- 原因：`setWidgetResizable(True)` 使滚动区域将网格组件拉伸填满视口高度，少图片时行被垂直拉伸
- 修复：网格布局设置 `AlignTop` 对齐，网格组件设置 `SizePolicy.Maximum` 高度策略，仅占用内容所需高度

**#16 少图片布局不跟随**
- 文件：`ui/preview_panel.py`
- 原因：`add_image` 直接用 `addWidget(card, row, col)`，当图片少于可用列数时，卡片只占前几列，评分和选择框位置不随窗口宽度调整
- 修复：提取 `_relayout_grid()` 方法，所有列设置 `setColumnStretch(c, 1)` 实现均匀分布；覆写 `resizeEvent` 窗口大小变化时重新布局

**#17 阈值条无法拖动**
- 文件：`ui/sidebar.py`
- 原因：`JumpSlider.mousePressEvent` 中点击跳转后直接 `event.accept()`，未调用 `super()`，导致 Qt 拖动跟踪机制未初始化
- 修复：点击跳转后调用 `super().mousePressEvent(event)`，让 Qt 默认处理器设置拖动状态

**#14 阈值条点击不跳转**
- 文件：`ui/sidebar.py`
- 原因：QSlider 默认点击轨道是步进（page step），不是跳转到鼠标位置
- 修复：新增 `JumpSlider` 子类，重写 `mousePressEvent`，点击时直接计算鼠标位置对应的值并设置

**#15 阈值变化自动选中图片**
- 文件：`ui/main_window.py`
- 原因：`_on_threshold_changed` 中调用 `select_above_score(threshold)` 自动勾选高于阈值的图片
- 修复：移除 `select_above_score` 调用，阈值变化仅更新金色框和统计信息，选择操作完全由用户手动控制

### 2026-05-26

**#1 PyQt6 启动报错**
- 原因：直接 `python3 main.py` 使用系统 Python，未安装 PyQt6
- 修复：使用虚拟环境 `./venv/bin/python main.py` 或先 `source venv/bin/activate` 再 `python main.py`

**#2 AI测试连接崩溃**
- 文件：`ui/main_window.py`
- 原因：`_test_ai_connection` 未处理 httpx 异常，连接失败时程序崩溃
- 修复：添加 try-catch 包裹异步调用，异常时返回 False 并提示用户，finally 中关闭 agent

**#3 AI配置默认显示**
- 文件：`ui/sidebar.py`
- 原因：AI 模型配置 GroupBox 默认可见，容易误触发
- 修复：默认隐藏 `model_group.hide()`，监听模式切换信号，选择 AI 模式时 `model_group.setVisible(True)`

**#4 图片加载卡在11%**
- 文件：`core/image_loader.py`
- 原因：批次触发逻辑仅在 `_loaded_count % THUMBNAIL_LOAD_BATCH == 0` 时推进，加载失败的图片不计数，导致批次卡住
- 修复：改用 `_processed_count` 统计已处理数（成功+失败），错误也触发 `_check_batch_complete()` 推进批次

**#5 图片加载数量不对（161→159）**
- 文件：`utils/cache.py`
- 原因：2 张 RGBA 模式的 PNG 图片在保存缩略图时，JPEG 不支持 RGBA 模式，抛出 `cannot write mode RGBA as JPEG`
- 修复：`create_thumbnail` 中检测 RGBA/P/LA 模式，先转 RGB 再保存

**#6 导出按钮不可点击**
- 文件：`ui/main_window.py`
- 原因：`_on_all_loaded` 中 `enable_export(False)` 禁用了导出按钮，筛选完成后未重新启用
- 修复：在 `_on_score_result` 和 AI 筛选完成时调用 `enable_export(True)`

**#7 切换模式后评分来源被覆盖**
- 文件：`ui/main_window.py`
- 原因：`_on_score_result` 根据 `self._current_mode` 动态判断 source，切换模式后所有评分都显示新模式的来源
- 修复：新增 `_score_sources` 字典，评分时记录实际来源，切换模式不影响已评分图片的来源显示

**#8 AI地址为空测试连接崩溃**
- 文件：`ui/main_window.py`
- 原因：仅检查空字符串，未校验 URL 格式，无效 URL 导致 httpx 异常
- 修复：添加 `api_url.startswith(("http://", "https://"))` 校验，不合法时提示用户

**#9 DeepSeek AI 筛选崩溃**
- 文件：`core/ai_agent.py`, `ui/main_window.py`, `ui/sidebar.py`
- 原因：模型名硬编码为 `"default"`，DeepSeek 需要具体模型名；AI 响应可能包含 markdown 代码块，直接 json.loads 会失败；连续失败时无熔断机制
- 修复：
  - 侧边栏新增「模型名称」输入框，支持 `deepseek-chat` / `deepseek-vl` 等
  - AI 响应 JSON 解析增加容错（支持 markdown 代码块、文本中提取 JSON）
  - 连续 3 次失败自动停止筛选并弹窗提示，避免程序崩溃

**#10 日志系统缺失**
- 文件：`utils/logger.py`（新建）, `config.py`, `main.py`, `ui/main_window.py`, `core/ai_agent.py`, `core/image_loader.py`, `core/exporter.py`, `utils/thread_manager.py`, `utils/cache.py`
- 原因：项目无任何日志输出，异常被静默吞掉，排查问题困难
- 修复：
  - 新增 `utils/logger.py`，使用 `RotatingFileHandler`（5MB，3 备份）写入 `~/.shiying/logs/shiying.log`
  - `config.py` 新增 `LOG_DIR` 常量
  - `main.py` 入口处调用 `setup_logging()`
  - 6 个模块共 12 处添加 `logger.error()` / `logger.warning()` / `logger.info()` / `logger.debug()` 调用

**#11 API 地址格式错误导致 404**
- 文件：`ui/main_window.py`, `core/ai_agent.py`, `ui/sidebar.py`
- 原因：用户输入 `https://api.deepseek.com/anthropic` 等错误地址，请求发到不存在的端点返回 404
- 修复：
  - `main_window.py` 添加 URL 格式校验，要求包含 `/chat/completions` 或 `/v1/`
  - `ai_agent.py` 的 `test_connection` 方法优化 URL 转换逻辑，支持多种 URL 格式
  - `sidebar.py` 添加 tooltip 提示常见 API 地址格式

**#12 纯文本模型发送图片请求导致 400**
- 文件：`core/ai_agent.py`, `ui/sidebar.py`
- 原因：`deepseek-chat` 是纯文本模型，不支持图片输入，但代码发送了包含 `image_url` 的请求
- 修复：
  - `ai_agent.py` 添加 `_is_vision_model()` 方法，通过关键词（vl/vision/gpt-4o 等）判断模型是否支持图片
  - 非视觉模型调用 `score_image()` 时抛出 `ValueError`，提示用户使用视觉模型
  - `sidebar.py` 更新 placeholder 和 tooltip，明确提示需要视觉模型

**#13 DeepSeek API 不支持图片分析**
- 文件：`core/ai_agent.py`, `ui/sidebar.py`
- 原因：DeepSeek API 仅支持文本内容，不支持 `image_url` 类型。API 返回错误：`unknown variant 'image_url', expected 'text'`
- 修复：
  - `ai_agent.py` 添加 `TEXT_ONLY_PREFIXES` 集合，将 `deepseek` 标记为不支持图片
  - `_is_vision_model()` 方法先检查是否为已知纯文本提供商
  - 错误信息明确说明 DeepSeek 不支持图片，并建议使用 OpenAI/Anthropic/Google
  - `sidebar.py` 更新 placeholder 和 tooltip，说明 DeepSeek 不支持图片分析
  - 添加 API 错误响应体日志，便于排查
