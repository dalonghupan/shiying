# 拾影·需求清单

**用途**：记录新需求和功能迭代计划，开发时按此文档逐项实现
**状态说明**：📋 待开发 | 🔧 开发中 | ✅ 已完成 | ❌ 不再需要

---

## 需求列表

| # | 状态 | 优先级 | 模块 | 需求描述 | 备注 |
|---|------|--------|------|----------|------|
| 1 | ✅ | - | 启动 | venv 虚拟环境启动说明 | 已记录在测试问题文档 |
| 2 | ✅ | P2 | 统计信息 | 添加高于设置的阈值照片统计 | 侧边栏新增"高于阈值: X"显示 |
| 3 | ✅ | P1 | 筛选图片 | 增加全选功能（再次点击则取消全选） | 工具栏添加全选按钮 |
| 4 | ✅ | P1 | 筛选图片 | 增加排序功能（按分数降序/升序） | 工具栏添加排序按钮 |
| 5 | ✅ | P1 | AI模型配置 | 内置当前流行的大模型配置，通过下拉框选择模型，只需要输入对应的APIkey。选择对应的模型，仅显示该模型可用的视图模型 | 提供商下拉框 + 模型下拉框 |
| 6 | ✅ | P3 | 窗口 | 页面优化。当前页面过于老气，设计增加一些年轻感 | 现代蓝紫色主题 |

---

## 优先级说明

- **P0**：必须立即修复，阻断核心流程
- **P1**：重要功能，下一轮迭代优先实现
- **P2**：体验优化，有空再做
- **P3**：锦上添花，可做可不做

---

## 迭代记录

（按时间倒序排列，每次迭代记录改动范围和完成情况）

### 2026-05-26 需求迭代 #2

**完成需求**：#6

**改动范围**：
- `config.py` — 颜色常量更新为蓝紫色系（主色 #7C4DFF，辅助色 #B388FF）
- `assets/styles.qss` — 全局样式重写：圆角 8-10px、紫色渐变进度条/滑块、指定字体、增大间距
- `ui/main_window.py` — 移除 `_apply_styles()` 中重复的内联 QSS
- `ui/sidebar.py` — 辅助文字颜色更新为 #8E8EA0
- `ui/preview_panel.py` — 评分标签颜色更新为 #8E8EA0
- `ui/toolbar.py` — 目录标签颜色更新为 #8E8EA0

**备注**：
- 采用蓝紫色调（#7C4DFF 主色），替代原有粉色薰衣草色
- 按钮改为紫色背景白字，圆角 8px，hover/pressed 状态加深
- GroupBox 圆角 10px，标题使用紫色
- 进度条和滑块使用紫色渐变
- 指定 SF Pro Text / PingFang SC 字体栈
- 统一辅助文字颜色为 #8E8EA0

### 2026-05-26 需求迭代 #1

**完成需求**：#2、#3、#4、#5

**改动范围**：
- `ui/toolbar.py` — 新增"全选"按钮和"排序 ↓/↑"按钮，支持切换状态
- `ui/preview_panel.py` — 新增 `toggle_select_all()`、`sort_by_score()` 方法
- `ui/sidebar.py` — 新增提供商下拉框（OpenAI/Anthropic/Google/自定义）和模型下拉框，自动填充接口地址；统计信息新增"高于阈值"显示
- `ui/main_window.py` — 连接全选和排序信号，`_update_stats()` 计算高于阈值数量

**备注**：
- 排序支持升序/降序切换，按钮文字动态变化
- 全选为 toggle 模式，再次点击取消全选
- 提供商下拉框自动填充对应 API 地址和可用视觉模型列表
- "自定义"模式允许手动输入任意接口地址
